<form method="post" action="{{ path('app_alimentation_jour_delete', {'id': alimentation_jour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alimentation_jour.id) }}">
    <button class="btn">Delete</button>
</form>
